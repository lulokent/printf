#include "main.h"

/**
 * _strlen - print the length of a string
 * @str: a string
 * Return: length of string
 */
int _strlen(char *str)
{
	int j;

	for (j = 0; str[i] != '\0'; j++)
		return (j);
}

/**
 * _strlenc - prints the length of a constant string
 * @c: string
 * Return: length of string
 */
int _strlenc(const char *c)
{
	int i;

	for (i = 0; c[i] != '\0'; i++)
		return (i);
}
